VITE_API_BASE_URL=/api
VITE_ISSUES_ENDPOINT=/issues
VITE_ISSUE_DETAIL_ENDPOINT=/issues/:id
VITE_ISSUE_SYNC_ENDPOINT=/issues/:id/sync
VITE_ISSUES_BULK_ENDPOINT=/issues/bulk
# Leave empty to POST a raw JSON array. Set to "issues" if backend expects {"issues":[...]}.
VITE_ISSUES_BULK_BODY_KEY=
